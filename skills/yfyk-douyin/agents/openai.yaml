interface:
  display_name: "YFYK Douyin"
  short_description: "在聊天内选纯外框模板，将 DOCX 转为完整抖音图文"
  default_prompt: "Use $yfyk-douyin to preview the actual first source page with frame-only templates, materialize a fixed or balanced-random outer-frame style, and convert these DOCX files into editable Douyin posts with complete embedded source-page cards."

policy:
  allow_implicit_invocation: true
